# 本地 Gemini 生图 / 反推：由 `api/gemini.ts`、`api/gemini-text.ts` 读取（Vercel dev 或线上部署）。
# 复制为 `.env.local`（与 package.json、vercel.json 同级），勿提交密钥。
GEMINI_API_KEY=

# 若 Google 控制台里变量名不同，也可使用：
# GOOGLE_API_KEY=

# 前端（如仅部署 .MTM）与 API 不在同一域名时，在 MTM 的 .env 中设置（不要尾斜杠）：
# VITE_API_BASE_URL=https://你的含-api-的项目.vercel.app

# 首页封面视频（腾讯云 COS 公网地址，可选；不设则用默认 lokada bucket）
# VITE_COVER_VIDEO_URL=https://lokada-1254090729.cos.ap-shanghai.myqcloud.com/NEW%20VERSION%20TRY%20ON/%E5%B0%81%E9%9D%A2.mp4
